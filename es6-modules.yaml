# Builds upon es6.yaml, so make sure to mix that in.

parserOptions:
  sourceType: module

rules:

  ## Strict Mode
  ## -----------

  # https://eslint.org/docs/rules/strict
  #   require or disallow strict mode directives
  strict:
    - error
    - never

